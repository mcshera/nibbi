# HEARTBEAT checklist

On each pulse: check items below. If nothing needs attention, reply exactly `HEARTBEAT_OK` and stop.

1. Unfinished fixer branches? (check `~/.nibbi/fixers.json` — `failed` with an empty `diffstat` = infra failure, safe to re-dispatch; `done` merges itself in ship mode)
2. Anything in inbox/ older than 24h unfiled?
3. Anything {{OWNER}} asked me to watch? (list below)

## Watching
- (nothing yet)
